# Generated by Cargo
# will have compiled files and executables
/target/

# Generated by Tauri
# will have schema files for capabilities auto-completion
/gen/schemas

# Tauri sidecar binaries staged at build time (macOS/Windows loom-voce helper). CI builds the
# helper and drops `binaries/loom-voce-<triple>` here before `tauri build`; never committed.
/binaries/
