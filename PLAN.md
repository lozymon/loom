# Termhaus — Terminal Control Room

> A Linux-first desktop app for running many real terminals at once, arranged in resizable split grids and a left workspace rail — a GUI terminal multiplexer (think tmux/Terminator) tuned for driving fleets of CLI agents. Built on Tauri 2 + SolidJS + xterm.js, with PTYs managed in Rust.

## Context
A "control room" of live terminals. Each pane is a real pseudo-terminal running any command — a shell, `claude`, a dev server, `tail -f`, `vim`. You split panes into a resizable grid, group them into Workspaces on a left rail, and (the headline trick) **broadcast one input to many panes at once** to launch and prompt a whole fleet in lockstep. Generic first: agents are just the most interesting thing you can run in a pane, not a special case baked into the model. This doc is the executable, incremental build plan; it front-loads the riskiest assumption (high-throughput PTY output rendering smoothly through Tauri IPC into WebKitGTK).

**Locked-in stack:**
- **Shell:** Tauri 2 (Rust + system WebKitGTK webview) — small bundles, fast on Linux.
- **Frontend:** SolidJS + Vite + TypeScript. Terminal rendering via **xterm.js** (`@xterm/xterm` 5.x).
- **PTYs:** Rust **`portable-pty`** (wezterm crate) — one PTY + child process per pane.
- **Persistence:** local JSON config (serde) for workspaces/layouts. SQLite only if/when searchable scrollback or session logging is wanted (deferred).

**Environment:** Node v24, Rust 1.96/cargo, Linux Mint 22.3 (apt). Project lives at `/home/lozymon/code/agentdeck/` (directory still named `agentdeck` — rename to `termhaus` is optional, see below).

## Architecture
```
SolidJS webview ── invoke(commands) ───────────────────────► Rust (Tauri core)
   xterm.js per pane                                          │  owns: PtyManager,
   layout tree (splits)   ◄── Channel<bytes> (PTY output) ────┤  one PTY + reader thread
   workspace rail (left)                                      │  per pane, resize, kill,
   broadcast input                                            │  workspace JSON persistence
        │                                                     ▼
        └── invoke pty_write / pty_resize ──────►  N× PTY masters → child procs
                                                   ($SHELL, claude, anything)
```
**One PtyManager owning `HashMap<PaneId, PtySession>`** — each session is a PTY master, its child process, and a dedicated reader thread that streams output bytes to the webview over a **Tauri `Channel`** (not events — channels are the fast binary-streaming path). Input (keystrokes) and control (resize/kill) flow the other way as cheap Tauri commands. This is symmetric to the old "one sidecar, N sessions" idea but the unit of isolation is now an OS PTY, which the kernel isolates for free.

### Language boundary (Rust does the core now)
The old (pre-pivot, SDK-based) plan kept Rust to glue; this pivot moves the engine into Rust because PTYs are an OS concern. Split:
- **Rust — the PTY engine.** `PtyManager`: spawn a PTY with a requested command/cwd/env, run the child, pump master→Channel on a reader thread, accept writes/resizes/kills, clean up on child exit. Plus workspace JSON load/save and packaging path resolution. Still no *product* logic — layout math, focus, broadcast routing all live in TS.
- **TypeScript — all UX/state.** SolidJS owns the layout tree (split geometry, drag-resize), the workspace rail, focus/zoom, the broadcast router, and binding each xterm instance to its pane's Channel. xterm.js owns ANSI parsing, scrollback, selection, rendering.
- **Shared types** (`PaneId`, `PaneSpec`, `LayoutNode`, command/payload shapes) live in one TS module (`src/ipc/protocol.ts`); the Rust command signatures mirror it.

### Data flow & performance (the make-or-break)
Terminal output is bursty and high-volume (`yes`, `cat bigfile`, a noisy build). The risk is saturating the Tauri IPC bridge or the WebKitGTK main thread. Mitigations, designed in from M0:
- **Output:** each PTY reader thread reads into a buffer and **coalesces** on three dimensions, flushing on whichever trips first — a ~8–16ms **tick**, a 32–64KB **buffer size**, and a **per-flush byte cap** (a flooding pane drops intermediate already-scrolled-off bytes rather than shipping all of them; xterm only needs to reach the correct screen state). Caps both frames-per-second and bytes-per-flush per pane regardless of throughput. Send raw bytes; avoid per-line JSON.
- **Transport:** **Tauri `Channel` first** (idiomatic, no open port/auth surface), but M0 is a spike — Tauri IPC may base64-encode channel payloads into WebKitGTK. Named fallback if Channel can't meet the M0 bar: a **localhost WebSocket** served by Rust streaming true binary frames. Callers talk to a thin `ptyClient` abstraction so swapping transport never touches them. See [ADR-0003](docs/adr/0003-output-transport-channel-first-websocket-fallback.md).
- **Render:** feed chunks straight into `term.write()` — xterm.js has its own internal write buffer and async flush, so don't pre-split into per-token DOM work. **Renderer: canvas-leading** (`@xterm/addon-canvas`), **DOM** as the ultra-safe fallback, **WebGL experimental opt-in only** (never default — most likely to fail silently on WebKitGTK). M0 picks the winner empirically and records it. See [ADR-0006](docs/adr/0006-canvas-renderer-not-webgl.md).
- **Input:** keystrokes → `pty_write(paneId, bytes)` command. Low volume, fine as plain commands. Never round-trip output through commands.
- **Backpressure:** bounded per-pane buffers; if a pane floods, drop-coalesce rather than queue unboundedly. A hidden Workspace's panes keep their PTY alive but throttle their flush rate.
- **Limits:** **no hard cap** on panes/Workspaces — bound cost by *visibility*, not count. A collapsed pane or hidden-Workspace pane isn't in the DOM (Solid `<Show>`) so it never *paints*; Rust keeps its output coalescing at a throttled rate so the xterm is current on reveal. The wizard's "12" is a creation-convenience ceiling, not a system limit. If a real WebKitGTK ceiling emerges in testing, **soft-warn** (don't forbid) and record the finding.

### Layout model
The **workspace rail** is a left vertical list (not top tabs). Each entry is a **Workspace** — a named container carrying a `working folder`, a **layout tree**, and a live terminal-count badge, with a close ✕. Switching the rail swaps the whole grid; PTYs in hidden Workspaces stay alive. Hierarchy is exactly two levels (Workspace → Pane); there is no intermediate "window" layer. Each tree leaf is a **named Pane** bound to a PTY.
```ts
type LayoutNode =
  | { kind: 'leaf'; paneId: PaneId }
  | { kind: 'split'; dir: 'row' | 'col'; ratio: number; a: LayoutNode; b: LayoutNode };
type Workspace = { id: string; name: string; cwd: string; tree: LayoutNode; panes: Record<PaneId, PaneSpec> };
```
- Splitting a leaf replaces it with a `split` whose children are the old leaf + a new leaf (new PTY). The new pane runs the **default `$SHELL`** (a split is "another terminal here," not a clone of the focused pane's command); default split direction is **split-right** (vertical divider, side-by-side). Closing a leaf collapses its parent split, promoting the sibling.
- Draggable dividers mutate `ratio` (0–1). Each pane measures its pixel box with `@xterm/addon-fit` → derives cols/rows → `pty_resize(paneId, cols, rows)`. Resize is debounced.
- Recursive SolidJS `<LayoutNode>` component renders splits as flex containers with a draggable gutter between `a` and `b`. Workspaces lazily mount panes but keep PTYs running when hidden.
- **Each pane has a title bar:** a **name** + per-pane controls (collapse / expand-split / maximize-zoom / close). The focused pane shows a focus ring. The name is auto-assigned from a curated pool of short distinct human names (Faye, Cleo, Wade…), **unique within the Workspace** (overflow → `Pane N`), purely a label (identity is the `PaneId`), **persisted** in `PaneSpec.title`, and **user-renamable** (double-click). Distinct names matter precisely because a fleet of panes often runs the *same* command (`claude` ×6) and would otherwise be indistinguishable.
- **Input routing:** **click-to-focus**, exactly one focused Pane; all keystrokes route to its PTY via `pty_write`. Mouse wheel scrolls that Pane's xterm scrollback locally (reaches the PTY only when the inner app enables mouse mode). App shortcuts occupy **only the `Ctrl+Shift+<key>` namespace** (`C/V` copy/paste, `D` split, `W` close, `←/→/↑/↓` move focus, `T` new Workspace…); every other combo — plain `Ctrl+C` (SIGINT), `Ctrl+B`, arrows, function keys — passes through untouched. No prefix key. See [ADR-0005](docs/adr/0005-ctrl-shift-shortcut-namespace.md).

### New-workspace wizard (the **+** on the rail)
Clicking **+** on the workspace rail opens a 3-step setup wizard (**Start → Layout → Agents**), mirroring the reference app:
1. **Start** — pick the **working folder** (where every terminal in this workspace starts). Surfaces **Recent workspaces** (folder + remembered terminal count) and **Presets** (saved configs) for one-click relaunch.
2. **Layout** — a row of **grid-preset tiles**: 1, 2, 4, 6, 8, 10, 12 terminals. Tapping a tile builds a **width-biased** balanced grid via `buildBalancedTree(n)` — `rows = floor(√n)`, `cols = ceil(n/rows)`, so tiles stay as wide as possible: 2→1×2, 4→2×2, 6→2×3, 8→2×4, 10→2×5, 12→3×4 (rows×cols). The freeform splitter still works after launch; this is the fast on-ramp, not a separate mode.
3. **Agents** *(optional)* — assign a launch command per pane (default `$SHELL`; one-click presets like `claude`). Exit via **"Open without AI"** (all panes = plain shells) or **"Next: Add AI agents"** then launch.
A workspace can be saved as a **Preset** (name + folder + layout + per-pane commands) for one-click relaunch from step 1.

### Shared protocol (Tauri commands + Channel)
- **Commands (TS→Rust):** `pty_spawn(spec: PaneSpec) -> PaneId` (returns id + opens its output Channel), `pty_write(paneId, bytes)`, `pty_resize(paneId, cols, rows)`, `pty_kill(paneId)`, `pty_list()`, `workspace_load() -> Workspace`, `workspace_save(ws)`.
- **PaneSpec:** `{ command?: string; args?: string[]; cwd?: string; env?: Record<string,string>; cols: number; rows: number }`. A plain Pane spawns the user's **login + interactive `$SHELL`** (e.g. `bash -l`); a Pane *with* a command runs it via `$SHELL -lc "<command>"` (not a direct `execvp`) so PATH + shell init load — see [ADR-0004](docs/adr/0004-launch-via-login-interactive-shell.md). Env = app base env + per-pane `env` + injected `TERM=xterm-256color`, `COLORTERM=truecolor`. Command-exit → Dead pane.
- **Channel (Rust→TS):** raw output bytes per pane, coalesced. Plus a small `pty_exit` event/channel frame carrying exit code when the child dies, so the UI can mark the pane dead and offer respawn.
- **Hard rule:** the output Channel carries terminal bytes verbatim — Rust never injects logs into it. Rust's own logging goes to stderr/tracing only.

## Workspace persistence (JSON, not SQLite — for now)
Persist layout + pane intent, never live scrollback. Three serialized things in the app-config dir:
- **`workspaces.json`** — the open rail: each Workspace's name, working folder (`cwd`), `LayoutNode` tree, and per-pane `PaneSpec` (command, **launch** cwd, env, title). On launch, rebuild each tree and **respawn** each pane's command in its **launch cwd** — the Workspace folder or the pane's override, *never* the live wandered cwd (Termhaus does not read `/proc/<pid>/cwd`; panes stay opaque per ADR-0001). Terminals are not restored to prior output — that's ephemeral.
- **`recents.json`** — recently opened working folders + their remembered terminal count, for the wizard's Start step.
- **`presets.json`** — saved one-click configs (name + folder + layout + per-pane commands).

SQLite is deferred to a later milestone only if searchable scrollback / session logging becomes a goal; JSON keeps Rust thin and the config human-inspectable.

## Failure handling
**One principle: every failure degrades to a Dead pane with a visible reason — never crash, never block sibling panes.**
- **Best-effort spawn:** panes spawn independently; one failure doesn't stop the others. Rust never panics on a spawn failure — it emits a pane-error event the UI renders as a Dead pane.
- **Command not found / immediate exit:** needs *no* special handling — a payoff of the login-shell launch (ADR-0004). `$SHELL -lc "claude"` with no `claude` prints `command not found` *into the pane* and exits → an ordinary Dead pane showing the error + restart.
- **Missing working folder** (deleted/unmounted on respawn): don't refuse to open — surface a Workspace-level warning and fall back to spawning in `$HOME`, marked with the reason; restart lets you repoint it.
- **OS-level spawn failure** (out of ptys, permissions): Dead pane carrying the OS error string, logged to stderr.

## Window model
**One OS window**, two layout regions for v1: **left workspace rail | center pane grid**. A single webview, one render tree — keeps the store and PTY→view binding simple.

## Out of scope for v1 (explicit no's)
These are deliberate exclusions, each recorded where relevant. Listed here so scope is unambiguous:
- **Agent-awareness** — no output parsing, token counts, or status badges; panes are opaque (ADR-0001).
- **Right-side Browser panel** — dropped for now. The reference app's `localhost`/docs preview is a whole second surface (embedded navigable webview) off the critical path; users alt-tab to a real browser. Revisit post-v1.
- **Detach/reattach daemon** — quitting kills all PTYs; persistence = respawn, not live-process survival (ADR-0002).
- **Multi-window & tear-off panes** — a Pane lives in one Workspace in the one window; multi-monitor/tear-off is a later feature.
- **tmux-style "window" layer** — hierarchy is two levels (Workspace → Pane) only.
- **Live-keystroke broadcast mirror** — broadcast sends discrete messages, not synchronized typing.
- **SQLite / searchable scrollback** — JSON persistence only until a logging need appears.

## Files to create (highest-leverage)
- `src-tauri/src/pty.rs` — `PtyManager`: spawn (portable-pty), reader-thread + coalescing flush to Channel, write/resize/kill, child-exit reaping.
- `src-tauri/src/commands.rs` — Tauri command handlers + Channel wiring (the frontend contract).
- `src-tauri/src/workspace.rs` — load/save `workspaces.json` / `recents.json` / `presets.json` (serde).
- `src/ipc/protocol.ts` — shared types: `PaneId`, `PaneSpec`, `LayoutNode`, `Workspace`, `Preset`, command names.
- `src/lib/ptyClient.ts` — spawn a pane, bind its xterm to the output Channel, wire write/resize/kill.
- `src/components/Terminal.tsx` — wraps an xterm.js instance, fit addon, title bar + per-pane controls, selection/copy-paste, binds to a pane.
- `src/components/LayoutNode.tsx` — recursive split renderer + draggable gutters.
- `src/components/WorkspaceRail.tsx` — left vertical rail of workspaces; +new (opens wizard) / close / switch.
- `src/components/NewWorkspaceWizard.tsx` — Start (folder + recents + presets) → Layout (grid tiles) → Agents.
- `src/components/BroadcastBar.tsx` — type a message; send `text`+newline to all Live panes in the current Workspace (or a selected subset).
- `src/lib/grid.ts` — `buildBalancedTree(n)` width-biased (`floor(√n)` rows) constructors for the 1/2/4/6/8/10/12 presets + pane naming.
- `src/stores/workspace.ts` — normalized store: workspaces (rail), layout trees, panes, focus/zoom, broadcast selection.
- `src-tauri/tauri.conf.json` — packaging (.deb/AppImage, WebKitGTK dep, bundle config).

## Milestones (build order — riskiest thing first)
- **M0 — PTY spine + flood test (make-or-break).** Tauri 2 + Solid + Vite scaffold; one xterm.js pane; Rust spawns one PTY (`$SHELL`), streams output over the Channel with 3-dimension coalescing, keystrokes round-trip via `pty_write`, fit→`pty_resize` keeps cols/rows correct. **Acceptance bar (the gate):** (1) one pane running `yes` (unbounded flood) → keystroke echo < ~100ms, no main-thread block > 100ms, memory bounded; (2) 12 panes each streaming `find /` concurrently → all render, UI stays interactive, CPU sane. If the Channel misses this with coalescing + byte-cap, switch to the **WebSocket fallback** (ADR-0003) and re-test. *Do not build further until this passes on WebKitGTK.*
  - **Status (2026-06-05): M0 GATE PASSED — both bars, real WebKitGTK/Tauri stack.** Bar (1): single-pane `yes` flood holds at ~178 MB main / ~232 MB web RSS, ~19% main CPU, live smooth render. Bar (2): 12 panes each sustaining a `yes` flood for 24s hold at **~198 MB main (flat)** / ~970–1049 MB web (bounded, oscillating), ~124%/109% CPU — each pane renders only its own output (no cross-talk), UI interactive. Channel+base64 transport is sufficient (no WebSocket fallback). Required fixing an unbounded-queue/uncapped-send-rate memory leak in the first cut — fix is bounded `sync_channel` + 64KB per-frame cap + ~16ms pacing, using lossless PTY back-pressure rather than byte-dropping. See [ADR-0003](docs/adr/0003-output-transport-channel-first-websocket-fallback.md) "M0 result".
- **M1 — N panes, no cross-talk.** `PtyManager` with `HashMap<PaneId,…>`; spawn/kill many PTYs; each xterm bound to its own Channel; child-exit marks the pane dead + offers respawn. Prove 6+ concurrent noisy terminals stream independently without bleed or jank.
  - **Status (2026-06-05): DONE.** `pty_kill` + per-pane `on_exit` Channel (exit code) added; the reaper thread reports exit and removes the pane (`pty.rs`). Frontend: `killPane`/exit wired in `ptyClient.ts`, panes kill their PTY on unmount, and a dead pane shows a `[Restart]` overlay carrying the real exit code (verified: code 7 propagated, neighbour shell unaffected). Shared types extracted to `src/ipc/protocol.ts`. App renders a flat CSS grid of N panes (`App.tsx`) — placeholder until the M3 wizard. Verified at 4 and 12 panes with no cross-talk. *Layout is still a flat grid; the split tree is M2.*
- **M2 — Split layout.** Recursive `LayoutNode` tree; split a pane horizontally/vertically; draggable gutters mutate ratios; debounced fit→resize per pane; close-pane collapses the split. Prove a 2×2 and an asymmetric layout resize cleanly.
  - **Status (2026-06-05): DONE.** `LayoutNode`/`Workspace` types in `protocol.ts`; `buildBalancedTree(n)` + auto-name pool in `grid.ts`; geometry in `layout.ts`; `stores/workspace.ts` holds tree/panes/focus/zoom with split/close/setRatio/rename ops. Panes carry a title bar (auto-name Faye/Cleo… + split-right/split-down/zoom/close + focus ring, dbl-click rename) and `Ctrl+Shift+D/E/W/Enter` shortcuts (ADR-0005). Verified on the live stack: 2×2 init, split (correctly nested), asymmetric gutter ratios, zoom (full-bleed, others hidden-but-mounted), close+sibling-promotion. **Key design note:** the renderer is *not* a recursive flex tree — `LayoutNode.tsx` flattens the tree into one flat, PaneId-keyed layer of absolutely-positioned panes + a gutter layer (`lib/layout.ts`), so splitting/closing never remounts a leaf's `<Terminal>` and its PTY survives. A frontend `PaneId` (tree identity) is now distinct from the Rust `PtyHandle` (IPC), owned inside each pane. *Workspace rail + persistence = M3.*
- **M3 — Workspace rail + new-workspace wizard + persistence.** Left vertical rail of workspaces (count badge + close); **+ opens the Start→Layout→Agents wizard** — pick working folder (with Recents), pick a grid-preset tile (1/2/4/6/8/10/12 → balanced tree via `grid.ts`), optionally set per-pane commands, launch. Switch/close workspace (PTYs survive hide). `workspaces.json`/`recents.json` save on change + respawn-on-launch. Restart → rail + layouts return, panes re-run their commands.
  - **Status (2026-06-05): DONE.** Store refactored to `workspaces[]` + `activeId` (`stores/workspace.ts`); App renders one `LayoutView` per workspace, only the active shown — switching keeps hidden PTYs alive (verified: 4 Home shells stay alive while Proj is active). `pty_spawn` now takes `command`+`cwd` → command panes run `$SHELL -lc "<cmd>"` in the folder (verified: a `/tmp` command pane printed its output + pwd, plain panes prompt in `/tmp`). `WorkspaceRail.tsx` (name + live count badge + close + **+**) and the 3-step `NewWorkspaceWizard.tsx` (native folder picker via `tauri-plugin-dialog` + Recents; preset tiles; per-pane commands; "Open without AI"/"Launch"). Persistence: thin schema-agnostic Rust `state_save`/`state_load` (`workspace.rs`, app config dir) + debounced autosave + `init()` hydrate-or-seed gated before render. **Restart round-trip verified:** saved Home(5, incl. a split) + Proj(`/tmp`, command pane) → relaunch restored both trees/names/commands/cwd and the active workspace, respawning each pane's command in its cwd (6 shells, correct cwds). *Caveat: wizard steps 2/3 are code-complete but click-paths not injectable in this env; the `createWorkspace` launch action they call is exercised directly. Window model is single-window rail|stage; multi-window is out of scope (v1).*
- **M4 — Fleet ergonomics (the screenshot features).** Broadcast bar: type a message, write `text`+newline to all Live panes in the current Workspace (or a selected subset); current-Workspace scope only, Dead panes skipped, live-keystroke mirror deferred. Per-pane title bar with controls (collapse/split/zoom-to-fullscreen/close) + focus ring; named panes (Faye, Cleo…); save-workspace-as-Preset + one-click relaunch from the wizard. Prove "spawn 12 panes from a tile, broadcast one prompt to all."
  - **Status (2026-06-05): DONE.** Broadcast routing decoupled from the component tree via a **pane registry** (`src/lib/paneRegistry.ts`): each `<Terminal>` registers `{write, isLive}` on mount and unregisters on cleanup, so the broadcast bar reaches every live PTY without prop-drilling its `PtyHandle`. Store (`stores/workspace.ts`) holds the targeting *intent* — ephemeral per-workspace `broadcast: PaneId[]` subset (empty = all) + a global `broadcastSelecting` mode — and `broadcastTargets(ws)` returns row-major PaneIds (subset minus since-closed panes, else all); the *writes* go through the registry (PTY concerns stay out of the store, Dead panes skipped by `isLive()`). `BroadcastBar.tsx` (docked below the stage, active-workspace scoped): input (Enter sends `text`+`\r`), Targets select-mode toggle, live-reach badge, Send. In select-mode each pane title bar shows a ◉/○ target toggle + amber highlight. **Presets:** `saveCurrentAsPreset()`/`launchPreset()`/`deletePreset()` (persisted under `presets.json`); rail footer **⛁ Save** snapshots the active workspace (cwd + pane count + per-pane commands), wizard step 1 lists presets for one-click relaunch. **Verified on the live stack (12-pane workspace, each pane `cat >> out_$$.txt`):** broadcast-to-all reached **12/12** distinct PTYs (12 PID-named files each got `PINGXYZ`); a 3-pane subset broadcast reached **exactly 3/3** (only those files also got `SUBSET9`) — real `pty_write` through real children, not a UI simulation. Screenshot confirmed the 3×4 named grid, per-pane ◉/○ toggles + amber subset highlight, rail Save/＋, and the broadcast bar's `→ 3 live / 3 picked` reach indicator. *Verification used a build-flag-gated `onMount` harness driving the real store + registry (keystroke/click injection unavailable in this env, as in M1–M3); harness removed after, test state wiped. Pane title-bar controls + focus ring + named panes already landed in M2.*
- **M5 — Terminal polish.** Copy/paste (`Ctrl+Shift+C/V`; plain `Ctrl+C` stays SIGINT), scrollback + search (`@xterm/addon-search`), URL/link click, themes/fonts, unicode11, the full `Ctrl+Shift` shortcut set (split/close/move-focus/next-workspace) per ADR-0005. Iron out WebKitGTK selection + clipboard quirks.
  - **Status (2026-06-05): DONE.** Shared `lib/theme.ts` (font stack + 16-colour ANSI theme); `Terminal.tsx` now loads the **search**, **web-links** (links opened via the OS through `@tauri-apps/plugin-opener`), and **unicode11** addons (`allowProposedApi` + `unicode.activeVersion="11"`). **Clipboard goes through the Tauri `clipboard-manager` plugin** (OS clipboard — reliable under WebKitGTK, where `navigator.clipboard` is flaky); added the Rust plugin + `clipboard-manager:allow-read-text/-write-text` capabilities. The `Ctrl+Shift` handler (ADR-0005) now covers **C** copy-selection / **V** paste / **F** find / **D**·**E** split / **W** close / **Enter** zoom / **←↑↓→** spatial move-focus (`lib/layout.ts` `neighbor()` + store `focusDir`) / **PageUp·PageDown** prev/next workspace (`switchWorkspaceRelative`) / **T** new-workspace (window event → wizard); plain `Ctrl+C` is *not* intercepted, so it still reaches the PTY as SIGINT. Per-pane scrollback **search overlay** (⌕ / Ctrl+Shift+F) with live `findNext`/`findPrevious` + match decorations, Enter/Shift-Enter/Esc. **Verified on the live stack** (6-pane workspace, results captured to disk via a `cat` sink): clipboard roundtrip + paste-into-PTY (`M5_CLIP_TOKEN` echoed), plain Ctrl+C `0x03` → trap fired (`sig.txt`), `Ctrl+Shift+ArrowRight` moved focus `5→6`, `Ctrl+Shift+E` split (+1 pane spawned in cwd), `Ctrl+Shift+F` opened search and highlighted the live `needle-ABC123` match (screenshot). *Verification note: keystrokes were actuated by dispatching real DOM keydowns at the focused xterm textarea (xterm calls `customKeyEventHandler` first for every keydown — confirmed in source — so this exercises the true handler path); a `__keys` breadcrumb proved the events hit the correct panes. The only wrinkle was test-side: all workspaces stay mounted (hidden = `display:none`), so the harness had to scope its selector to the visible `.ws-layer`. Harness removed, state wiped after.*
- **M6 — Packaging.** `.deb` (primary) + AppImage; resolve `$SHELL`/PATH correctly from a bundled launch; declare the WebKitGTK apt dep; test on a clean Mint 22.3 VM with no dev toolchain.
  - **Status (2026-06-05): DONE (.deb built + release binary verified; AppImage configured but unbuildable in this sandbox).** `tauri.conf.json` bundle: `productName "Termhaus"`, targets `["deb","appimage"]`, `category "Utility"`, short/long descriptions; `Cargo.toml` description/authors filled. **`npm run tauri build` produced `Termhaus_0.1.0_amd64.deb`** (4.97 MB) — verified well-formed: package `termhaus`, `Depends: libwebkit2gtk-4.1-0, libgtk-3-0` (Tauri auto-detects the linked WebKitGTK version, so no explicit `deb.depends` is needed — adding them only duplicated the list), payload = `/usr/bin/termhaus` + hicolor icons (32/128/256@2) + `usr/share/applications/Termhaus.desktop` (`Exec=termhaus`, `Categories=Utility;`, `Terminal=false`). **The optimized release binary was launched standalone and renders the full app** (seeded Home workspace, 4 login-shell panes, rail, broadcast bar) — screenshot-confirmed. **`$SHELL`/PATH from a bundled launch:** every pane already spawns a *login* shell (`$SHELL -l`, ADR-0004), which re-sources the profile so PATH is correct even when started from a desktop launcher that inherits only the session env; hardened `pty.rs` with `resolve_shell()` (`$SHELL` if it exists → `/bin/bash` → `/bin/sh`) for a bare launch where `$SHELL` is unset. **AppImage:** target is configured and the bundler runs, but it failed here with `Network is unreachable` — it downloads AppRun/linuxdeploy/the gtk plugin from GitHub at bundle time, which this sandbox blocks; it will build in a networked dev/CI environment. *Not done in this environment: the clean-Mint-22.3-VM install test (no VM available here) — the artifact, deps, .desktop, and a standalone run of the packaged binary are all verified, but an actual `dpkg -i` on a toolchain-free machine remains the final acceptance step.*
- **M7 — Windows support (planned; the project stays Linux-first).** Bring the app up on Windows 10/11. Most of the stack is already portable: `portable-pty` uses **ConPTY** automatically (the M0 engine — reader/flusher coalescing, write/resize/kill, child reaping — is unchanged), Tauri's `app_config_dir()` resolves to `%APPDATA%\com.termhaus.app` (persistence works), the clipboard/opener/dialog plugins + all of xterm/addons are platform-neutral, and the webview is **WebView2 (Chromium)** rather than WebKitGTK — so the M5 clipboard/WebGL quirks largely don't apply. The Unix assumptions are localized:
  - **M7.1 — Platform-split shell spawning (`src-tauri/src/pty.rs`).** The launch model is the one Unix-only piece. Split `resolve_shell()` + arg building behind `#[cfg(unix)]` / `#[cfg(windows)]`. Windows has **no login-shell concept** (PATH comes from the env/registry, not a sourced profile — this generalizes [ADR-0004](docs/adr/0004-launch-via-login-interactive-shell.md), which stays the Unix rule): a plain pane runs **PowerShell** (`powershell.exe`, falling back to `%COMSPEC%`/`cmd.exe`), a command pane runs `powershell.exe -NoLogo -Command "<cmd>"` (cmd uses `/c`). cwd fallback becomes `USERPROFILE` (vs `HOME`); keep `TERM`/`COLORTERM` under `#[cfg(unix)]`. The `#[cfg]` split keeps the Linux build byte-identical.
  - **M7.2 — Frontend path normalization.** `NewWorkspaceWizard.tsx` `basename()` splits on `/` only; accept `\` too (`split(/[\\/]/)`). Audit other path/`/`-string handling (the rest is pure geometry / id parsing, already neutral).
  - **M7.3 — Windows packaging + CI.** Add a `tauri.windows.conf.json` overlay with `"targets": ["nsis"]` (NSIS installer; bundles the WebView2 bootstrapper automatically; `icon.ico` already present); the Linux `["deb","appimage"]` config is untouched. Add a `windows-latest` job to `.github/workflows/ci.yml` (no apt step; `tauri build --bundles nsis`).
  - **M7.4 — Verify on real Windows (can't be done from the Linux dev box).** ConPTY line-ending delivery (broadcast/Enter send `\r` — confirm PowerShell/cmd see Enter); plain `Ctrl+C` → ConPTY console interrupt (handler already passes it through); re-run the **M0 flood test** on WebView2 (`while($true){...}`); NSIS installer installs + launches on a clean Win10/11.
  - *Scope note: macOS is a likely-cheap follow-on (login shells + portable-pty work there; the open question is `Ctrl` vs native `Cmd` shortcuts) but is out of scope for M7.*
- **M8 — Source Control panel (VSCode-style git diff viewer).** A read-only diff viewer scoped to the **focused terminal's live working directory** (where you've `cd`'d to — falling back to the workspace's launch folder, then nothing), opened from a rail **⎇ Source control** button or **Ctrl+Shift+G**. A full-screen modal (same overlay pattern as Settings — *not* a `LayoutNode` leaf; panes stay opaque PTYs per [ADR-0001](docs/adr/0001-opaque-panes-no-agent-awareness.md)): a left file list grouped into **Staged Changes** / **Changes** (with status badges M/A/D/R/U), and a **side-by-side unified-diff** main pane. **Language boundary:** Rust (`src-tauri/src/git.rs`) is a thin, read-only shell-out — `git_status(cwd)` (resolves the repo root via `rev-parse --show-toplevel`, then `status --porcelain=v1 -z`) and `git_diff(cwd, path, staged, untracked)` (raw unified-diff text; untracked files diffed against `/dev/null`). All diff *parsing/alignment* is product logic in TS (`src/lib/gitClient.ts` `parseUnifiedDiff` → side-by-side rows; `src/components/GitPanel.tsx`). **Path subtlety:** porcelain paths are repo-root-relative and `git diff -- <path>` only resolves them from the root, so every command runs with the resolved root as cwd. **Live-cwd scoping:** the panel resolves its folder from the focused pane's shell via `pty_cwd(paneId)` → `/proc/<pid>/cwd` (the child pid is tracked in `pty.rs`); this is a deliberate, narrow carve-out from ADR-0001's "no `/proc` reads" rule, recorded in that ADR's 2026-06-06 amendment — it reads kernel process state on an explicit action, never pane output. *Scope: read-only — no stage/unstage/commit, no inline editing, no per-hunk actions (deliberately deferred; the viewer is the headline). Linux-first like the rest of the app (`/dev/null` is Unix-only — a Windows `nul` path follows the M7 `#[cfg]` pattern when needed).*
  - **Status (2026-06-06): DONE (code-complete; live click-path verification pending as in M1–M5).** `git.rs` + commands registered in `lib.rs`; `gitClient.ts` parser **validated** against real mixed `git diff` output (top-insert → empty-left add rows, context lines tracking old/new line numbers across multiple hunks, appends → right-side add rows). `GitPanel.tsx` wired into `App.tsx` (signal + `termhaus:source-control` window event), rail button in `WorkspaceRail.tsx`, and a `source-control` keybinding (default `Ctrl+Shift+G`, dispatched from `Terminal.tsx`, merges cleanly over older saved keybinding blobs). **Live-cwd scoping added** after first-cut feedback (the panel was blank because the seeded "Home" workspace has `cwd: ""`): `pty.rs` tracks the shell child pid + `pty_cwd` reads `/proc/<pid>/cwd`; the pane registry exposes `paneCwd(paneId)`; `GitPanel` resolves focused-pane live cwd → workspace folder, and shows the resolved folder in its header. `npx tsc --noEmit` + `cargo check` both clean.

## Top risks
1. **[HIGHEST] PTY throughput → smooth render in WebKitGTK via Tauri IPC.** A flood (`yes`) can lock the webview main thread or balloon memory. Mitigate: Rust-side coalescing (time+size flush), raw-byte Channel (not JSON events), xterm's own write buffer, canvas renderer, bounded/drop buffers. **Prove in M0 before anything else.**
2. **Resize correctness.** xterm fit cols/rows must stay in lockstep with `pty_resize`, including reflow and rapid drags. Debounce; resize PTY *after* xterm fit; test odd aspect ratios.
3. **Renderer on WebKitGTK.** WebGL may be broken/slow — canvas leads, DOM is the safe fallback, WebGL is experimental opt-in only; M0 validates the pick (ADR-0006).
4. **Thread/handle scaling.** One reader thread per PTY × many panes. Bound buffers, throttle hidden Workspaces, reap exited children promptly; revisit async/epoll only if thread count bites.
5. **Input latency.** Keystroke → command → `pty_write` must feel instant. Keep the command path trivial; never batch keystrokes behind a tick.
6. **Clipboard & keybinding conflicts on Linux/WebKitGTK.** Ctrl-C ambiguity (SIGINT vs copy), primary-selection paste, xterm selection rendering. Budget M5 time for this.

**Verify against the live stack before finalizing M0:** Tauri 2 `Channel` byte-streaming API + payload shape; `portable-pty` master read/write/resize + child-exit reaping on Linux; `@xterm/xterm` 5.x renderer options + fit addon under WebKitGTK; whether the WebGL addon works at all in this webview.

## Verification
- **M0:** `npm run tauri dev` opens a window with a working shell; typing works; resizing the window keeps the prompt aligned; `yes` floods without locking the UI or leaking memory; 12×`find /` stays interactive. **This is the gate** — and it locks in the renderer (ADR-0006) and transport (ADR-0003) picks empirically.
- **M1:** spawn 6+ panes running different noisy commands; each renders its own stream; killing one (or its child exiting) marks only that pane dead.
- **M2:** split into a 2×2; drag gutters; every pane's app (e.g. `htop`) reflows to the new size correctly; closing a pane collapses cleanly.
- **M3:** build layouts across 2 Workspaces; restart the app; the rail + trees return and each pane re-runs its command in its cwd.
- **M4:** spawn 12 panes; type once in the broadcast bar; all 12 receive it; zoom one pane fullscreen and restore.
- **M5:** select + copy from one pane, paste into another; Ctrl-C still interrupts; scrollback search finds text.
- **M6:** install the `.deb` and run the AppImage on a clean Mint 22.3 VM; terminals work end-to-end with no dev toolchain installed.
- **M7:** on a clean Win10/11 box, install the NSIS package and launch; a pane opens a working PowerShell, Enter/broadcast reach the shell, plain `Ctrl+C` interrupts, and a flood (`while($true){...}`) stays smooth on WebView2.
